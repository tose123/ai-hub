interface:
  display_name: "同步 newapi main"
  short_description: "安全同步 newapi/main 并核验本地功能未被覆盖"
  default_prompt: "使用 $newapi-main-sync 同步 newapi/main 到本地 main，审计文本和语义冲突以及 merge 后修复，逐项确认本地功能仍被保留，再总结更新与验证结果。"
